extends layout 

block content 
  .w3-card-4
    header.w3-container.w3-teal
      h1 Aluno #{a.id}
    .w3-container
      ul.w3-ul.w3-card-4(style="width:50%")
        li 
          b Nome: #{a.nome}
        li 
          b Número: #{a.id}
        li 
          b Git (link): 
            a(href=a.gitlink)=a.gitlink
        each i in [1,2,3,4,5,6,7,8]
          - var key = "tpc" + i
          label TPC #{i}
            if(a[key] != undefined)
              input.w3-check(type="checkbox" name=key value="1" checked)
            else
                input.w3-check(type="checkbox" name=key value="1")              
    footer.w3-container.w3-teal
      address Gerado por galuno::RPCW2022 em #{d} - #[a(href="/") Voltar]
            